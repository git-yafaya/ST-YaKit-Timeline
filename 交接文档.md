# ST-yafaya-Timeline 项目交接文档

更新时间：2026-08-20  
当前分支：master  
当前 HEAD：acbe3dc 接入聊天状态与故事时间恢复

## 1. 项目位置与仓库状态

当前可读取的项目目录：

/home/yafaya/SillyTavern/docker/extensions/ST-YaKit-Timeline

此前工作区 /home/yafaya/agent_work/codex/ST-yafaya-Timeline 已不存在；同名旧目录位于回收站。后续应以当前扩展目录为准，不要直接修改回收站副本。

manifest.json 加载 dist/index.js，样式为 dist/index.css。插件显示名为“时间线”，不限制 SillyTavern 最低版本。

此前交接中的 6 个旧 HTML 原型删除已由 `10f238a 去除HTML` 提交：

- dist/ui/AI分析.html
- dist/ui/overview-review.html
- dist/ui/分组.html
- dist/ui/日志.html
- dist/ui/时间线.html
- dist/ui/设置s.html

当前工作区不再有这 6 个 HTML 删除变更，不要恢复这些原型文件。

## 2. 产品边界与安全原则

V1 只处理当前角色卡实际绑定的一本主世界书。

- 配置阶段可以使用 AI；运行阶段只能使用已确认的确定性配置。
- 未选择角色、未绑定世界书、世界书不可读时，停止扫描和自动控制。
- 不读取全局启用项、聊天额外挂载项或名称相似的其他世界书来猜测目标。
- 不修改世界书正文、Comment、Key、Order、Depth、Position 等原生字段。
- 自动控制未来只能修改当前角色绑定世界书中、用户确认纳管条目的 enabled/disable 状态。
- 不使用 localStorage 保存正式配置或聊天状态。
- 不记录 API Key、完整聊天正文或完整世界书正文。

权威需求基线是 ST-yafaya-Timeline 设计文档.md；执行约束是 AGENT.md。

## 3. 已完成内容

### UI 与 Shell

- 固定时间线管理器外层 Shell、窗口尺寸、顶部导航和状态栏。
- Tab 几何冻结；状态只改变颜色、边框、阴影和底部指示条。
- Tab 和内容区使用轻量过渡，不动画宽高、padding、字号或布局尺寸。
- 设置页改为居中单列 Accordion，保留所有设置项和业务事件。
- 时间线页、设置页使用自定义深色 Listbox 和低对比滚动条。
- Light / Dark / Follow 主题通过 CSS Variables Token 切换。

### 设置与 AI 接口

- 设置持久化到 extension_settings.st_yafaya_timeline.globalSettings。
- 支持跟随主 API / 副 API、模型获取、自定义 Listbox、连接测试和保存反馈。
- 默认 Temperature 0.9、最大输出 Token 23333、超时 180 秒。
- 主 API 模型留空时使用 SillyTavern 当前模型；选择模型时走 SillyTavern 本地后端。
- 副 API 通过 SillyTavern 后端代理请求，不从插件页面直接跨域访问外部 API。

### 当前角色世界书只读适配

文件：src/st/sillytavern-adapter.ts

- 读取 characters[characterId].data.extensions.world 的精确绑定名称。
- 只调用该名称对应的 loadWorldInfo(name)。
- 原生 disable 映射为只读快照中的 enabled。
- 角色、聊天和世界书事件变化时刷新状态。
- 适配器目前主要承担读取；正式自动切换写入能力尚未接入。

### 聊天状态与故事时间恢复

文件：src/storage/chat-state.ts、src/timeline/runtime.ts、src/ui/App.vue

- 聊天状态保存到 `chatMetadata.st_yafaya_timeline`，只通过 `updateChatMetadata` 与 `saveMetadataDebounced` 写入，不使用 `localStorage`。
- 严格校验当前故事时间、世界书标识、组模式、激活条目、倒退待确认状态和最近 100 条日志。
- 打开/切换聊天时优先恢复合法 metadata；metadata 缺失或无合法时间时，才从当前聊天最后一条 AI 消息恢复最后一个合法 `<wlog>`。
- 监听 `MESSAGE_RECEIVED` 处理已完成 AI 回复；流式期间不解析，末条消息无合法时间时沿用旧状态。
- “获取当前时间”只读取末条 AI 回复；同日只更新时间，倒退进入可持久化的确认/拒绝流程，大跨度前进显示提醒。
- 当前阶段仍不写入任何世界书原生字段。

### AI 扫描草稿

文件：src/analysis/scanner.ts、src/st/ai-adapter.ts

- 快速扫描按 world_timeline、日期密度和时间线语义做本地候选筛选。
- 深度扫描发送全部当前世界书条目。
- 超过 25 条时分批分析，再进行精简结果汇总。
- 严格校验 JSON、日期、entry ID、重复引用、置信度和警告。
- 校验失败最多重试 3 次，并可查看最后一次原始输出。
- 支持进度、取消和作用域版本保护；角色或世界书变化时丢弃过期结果。
- 草稿阶段不修改世界书。

### 首次“确认并应用”

文件：src/storage/worldbook-config.ts、src/ui/worldbook-view.ts

- 只允许首次确认保存；已有正式配置时禁止重新分析结果直接覆盖。
- 保存到 extension_settings.st_yafaya_timeline.worldbooks[worldbookKey]。
- 保存内容包括组、受管条目、SHA-256 内容 Hash、展示标题、人工锁定字段、置信度、有效起止日期和 stale 标记。
- 相邻条目的切换边界当天属于下一条；前一条终点为边界前一天，最后一条保持开放。
- 保存失败时恢复原命名空间。
- 总览页和时间线页读取正式配置，并结合当前世界书只读快照展示实际启用状态。
- 当前阶段不会写入世界书原生开关。

## 4. 关键文件地图

- src/index.ts：扩展入口、魔法棒菜单注册和 Shadow DOM 挂载。
- src/ui/App.vue：页面状态、宿主上下文、扫描和首次应用编排。
- src/ui/components/AppShell.vue：固定外层 Shell。
- src/ui/components/TopNav.vue：顶部主导航和“获取当前时间”入口。
- src/ui/components/ContextBar.vue：角色、世界书和故事时间状态栏。
- src/st/sillytavern-adapter.ts：当前角色主世界书只读适配器。
- src/storage/chat-state.ts：聊天 metadata 运行状态读写与严格校验。
- src/st/ai-adapter.ts：主 API / 副 API AI 请求适配。
- src/analysis/scanner.ts：候选筛选、批处理、JSON 校验和重试。
- src/storage/worldbook-config.ts：正式世界书配置构建、校验、读写。
- src/ui/worldbook-view.ts：正式配置到总览/时间线 UI 的投影。
- src/timeline/wlog.ts：wlog 纯解析器。
- src/timeline/date.ts：故事日期校验与比较。
- src/timeline/runtime.ts：末条 AI 恢复、时间更新和倒退确认纯逻辑。
- src/timeline/matcher.ts：确定性时间线匹配器。
- src/ui/settings-store.ts：全局设置持久化。
- src/styles/tokens.css：Light/Dark 语义 Token。
- src/styles/index.css：Shadow DOM 内部样式。

## 5. 当前持久化结构

全局设置：
extension_settings.st_yafaya_timeline.globalSettings

世界书公共配置：
extension_settings.st_yafaya_timeline.worldbooks[worldbookKey]

聊天运行状态：
chatMetadata.st_yafaya_timeline

聊天状态保存当前 StoryTime、每组 auto/manual 模式、激活条目、倒退待确认状态和最近 100 条运行日志。

## 6. 尚未完成的下一阶段

1. 自动引擎与安全切换器：使用 matchTimelineEntry 计算目标，只修改确认纳管条目；写入后重新读取校验，失败最多重试一次，组级故障隔离。
2. 聊天级模式与日志 UI：接入自动/手动切换规则、聊天运行日志和系统日志分离。
3. 重新分析差异合并：按 ID → 内容 Hash → 名称/内容相似度重匹配，保留人工锁定字段。
4. 分组页持久化：新建、重命名、合并、拆分、删除、排序及人工锁定同步。
5. 多标签页控制权：BroadcastChannel + heartbeat，失去锁后暂停自动写入。

## 7. 当前明确限制

- 目前没有自动引擎，不会因 wlog 自动切换世界书。
- 自动/手动组状态虽然已进入 chatMetadata 模型，但时间线页的模式切换与日志展示尚未接通完整业务。
- “确认并应用”只建立首次正式配置；已有配置的重新分析差异合并未实现。
- 时间线页展示当前原生 enabled 状态，但自动/手动模式目前是默认 auto 投影，聊天级模式接入后应替换。
- 尚未执行真实 AI 扫描；AI 请求使用单元测试模拟，避免未经确认发送用户世界书正文或消耗 Token。
- 尚未完成世界书 `enabled` 写入保存、多标签页竞争和移动端真实交互验收。

## 8. 验证与发布流程

在当前项目目录执行：

npm run typecheck
npm test
npm run build
git diff --check

最近一次记录：83 项测试通过，类型检查通过，Vite 构建通过，`git diff --check` 通过。

构建后按 AGENT.md 复制 manifest.json、dist/index.js、dist/index.css。历史部署目标为：

/home/yafaya/SillyTavern/docker/data/default-user/extensions/ST-yafaya-Timeline/

当前实际运行容器通过 compose 将 `docker/extensions` 直接挂载到 `/home/node/app/public/scripts/extensions/third-party`；本阶段已核对宿主源码产物与 `sillytavern` 容器内三份 SHA-256 一致。历史 `/home/yafaya/SillyTavern/docker/data/default-user/extensions/ST-yafaya-Timeline/` 目标目录当前不存在，不要擅自创建第二套部署路径。

每个边界清晰阶段都要完成：类型检查、测试、构建、差异检查；使用简体中文提交原因；复制并核对产物；在工作日志.md 记录验证内容。

## 9. 最近提交

- acbe3dc 接入聊天状态与故事时间恢复
- 10f238a 去除HTML
- 28b49f5 实现首次确认并保存时间线配置
- e761e2e 接通AI扫描草稿生成流程
- 46d1dfd 接入总览真实世界书状态
- 35981c2 接入当前角色世界书只读适配器
- 15f16c4 修复AI连接测试与保存反馈
- c743f4e 增加主副API模型选择功能
- 688fad4 补全插件设置持久化底层
- bacec74 调整时间跳跃提醒阈值选项

## 10. 接手后的第一步

下一阶段开始实现自动引擎与安全切换器：先补齐 `WorldInfoAdapter` 的最小 enabled 写入/保存/回读校验接口，再接 `matchTimelineEntry`、组级隔离和一次重试。不要修改正文或其他原生字段，也不要把“读取到的原生 enabled 状态”误当成插件已经接管。
