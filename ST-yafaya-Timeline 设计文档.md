# ST-yafaya-Timeline 设计文档

> 项目名称：**ST-yafaya-Timeline**  
> 界面显示名：**时间线管理**  
> 图标：Font Awesome `fa-timeline`  
> 目标平台：**SillyTavern（不限制最低版本）**
> V1 界面语言：**简体中文**  
> 文档状态：**V1 最终需求基线**

---

## 1. 项目概述

ST-yafaya-Timeline 是一个用于 **SillyTavern 世界书（World Info / Lorebook）时间线条目自动开关** 的第三方扩展。

插件解决的核心问题是：

> 同一角色世界书中存在多个描述不同剧情时期的时间线条目。随着剧情中的故事日期推进，需要自动关闭已经过期的时间线条目，并开启当前日期对应的时间线条目。

当前预设会在 AI 正文中持续输出类似：

```xml
<wlog time="🕒时间:424年5月14日/17:15">
【世界主线】
环球探索与世界漫游之旅 (424年5月10日起) ｜进行中
...
</wlog>
```

插件从 `<wlog time="...">` 中直接解析当前故事时间，只使用 **年 / 月 / 日** 参与时间线切换，时分仅用于显示与记录。

V1 的产品原则是：

> **AI 负责配置阶段的识别、整理和辅助判断；运行阶段尽量使用确定性规则，不依赖 AI 猜测。**

---

# 2. 核心设计原则

## 2.1 最小修改权限

自动模式下，插件只允许修改：

- 已经由用户确认纳入时间线管理的世界书条目的 **启用 / 禁用状态**

插件不得自动修改：

- 世界书正文
- 条目名称 / Comment
- Key / Secondary Key
- Constant / Selective
- Order
- Depth
- Position
- Probability
- Strategy
- 世界书绑定关系
- 其他任何原生世界书字段

插件生成的标题、分组、时间范围、人工修正等全部保存在插件自己的配置中，不写回原世界书。

---

## 2.2 只管理当前角色卡所属世界书

插件只认：

> **SillyTavern 当前角色卡实际绑定的世界书。**

不通过以下方式猜测：

- 角色名匹配
- 世界书名称相似度
- 当前全局启用的世界书
- 当前聊天额外挂载的世界书

V1 假定：

> 一张角色卡只绑定一本世界书。

如果当前角色卡没有绑定世界书：

```text
当前角色卡未绑定世界书
请先为角色卡绑定世界书后再使用时间线管理。
```

此时插件不扫描、不猜测、不修改任何世界书。

---

## 2.3 一个时间线组只属于一本世界书

时间线组绝不跨世界书。

即使未来角色卡世界书结构变复杂，也不得把不同世界书中的条目合并成同一时间线组。

---

# 3. 扩展架构

## 3.1 纯前端 SillyTavern 扩展

V1 采用纯 SillyTavern 第三方前端扩展架构：

- 不额外安装 Server Plugin
- 不维护自定义 `../Timeline/...` 文件目录
- 使用 SillyTavern 原生持久化能力
- 从 GitHub 仓库通过 SillyTavern 扩展管理器安装和更新

实现方式可参考 `ST-BaiBai-Book` 的扩展架构思路：

- 使用 SillyTavern `getContext()`
- 插件全局配置存入 `extension_settings`
- 聊天级运行状态存入 `chatMetadata`
- 魔法棒菜单中注入入口
- 推荐使用 Shadow DOM 隔离 UI

参考仓库：

```text
https://github.com/baibai-git/ST-BaiBai-Book
```

> 实际开发时以当前安装的 SillyTavern 的真实 API 与事件行为为准。`manifest.json` 不声明最低客户端版本；遇到具体版本兼容问题后再按实际故障修复，不预先堆叠兼容层。

---

# 4. 持久化模型

## 4.1 全局配置

建议命名空间：

```text
extension_settings.st_yafaya_timeline
```

全局配置负责：

- AI 模式
- 独立 OpenAI Compatible API 配置
- 扫描策略
- UI 主题
- Toast 设置
- 大跨度时间跳跃提醒阈值
- 系统日志
- UI 偏好
- 其他插件级设置

示意：

```json
{
  "globalSettings": {
    "theme": "follow",
    "switchToastEnabled": true,
    "largeJumpDays": 365,
    "ai": {
      "mode": "sillytavern",
      "openaiCompatible": {
        "baseUrl": "",
        "apiKey": "",
        "model": "",
        "temperature": 0.2,
        "maxTokens": 8192,
        "timeoutSec": 180
      }
    }
  }
}
```

---

## 4.2 世界书公共时间线配置

存储于：

```text
extension_settings.st_yafaya_timeline.worldbooks
```

该层回答：

> “这本世界书的时间线结构是什么？”

建议保存：

- 世界书识别信息
- 时间线组
- 条目映射
- 原条目 ID
- 原条目名称
- 内容 Hash
- AI 展示标题
- 人工展示标题
- 条目顺序
- 起始日期
- 切换边界
- 计算后的有效时间范围
- AI 置信度
- 人工确认状态
- 是否纳入时间线管理
- 人工锁定字段
- 内容是否已发生变化
- 日期异常警告
- 缺失边界推导结果
- 配置更新时间

---

## 4.3 聊天运行状态

存储于：

```text
chatMetadata.st_yafaya_timeline
```

该层回答：

> “这个聊天现在走到哪里、当前各组应该是什么状态？”

建议保存：

- 最近一次成功解析出的完整故事时间
- 当前年月日
- 当前角色世界书标识
- 各时间线组运行模式
- 各组当前激活条目
- 手动模式下的开关状态记录
- 时间倒退待确认状态
- 当前错误 / 警告状态
- 最近 100 条聊天运行日志

同一本世界书允许被多个聊天使用，每个聊天拥有独立状态。

例如：

```text
聊天 A
当前时间：424/05/14
主线：环球探索篇

聊天 B
当前时间：420/03/10
主线：西部港篇
```

世界书结构共享，但当前进度不共享。

---

# 5. 当前时间来源

## 5.1 唯一自动时间来源

运行阶段的当前时间只从：

```xml
<wlog time="...">
```

获取。

例如：

```xml
<wlog time="🕒时间:424年5月14日/17:15">
```

解析为：

```text
year   = 424
month  = 5
day    = 14
hour   = 17
minute = 15
```

自动切换只比较：

```text
424-05-14
```

不比较：

```text
17:15
```

---

## 5.2 当前时间不使用 AI 推断

运行阶段：

- 不让 AI 猜当前日期
- 不根据剧情内容推测日期
- 不根据“昨天 / 明天 / 数日后”等自然语言推断
- 不在 `<wlog>` 无效时调用 AI 兜底

`<wlog>` 解析失败就报错。

---

## 5.3 同一条回复存在多个 `<wlog>`

只处理最终 AI 消息。

如果同一条消息存在多个 `<wlog>`：

> 从消息末尾向前寻找最后一个可以成功解析的 `<wlog time>`。

如果最后一个损坏，但前一个合法，则使用前一个合法项。

---

## 5.4 `<wlog>` 缺失或损坏

以下情况视为无有效时间：

- 没有 `<wlog>`
- `time` 缺失
- 年月日无法解析
- 标签不完整
- 中途停止生成导致 `<wlog>` 残缺

处理原则：

> **绝对不要改变当前世界书开关。**

如果已有历史有效时间，则继续沿用，并提示：

```text
⚠ 本轮未检测到有效时间，已沿用上次时间。
请自行重 Roll 或补充时间。
```

如果是新聊天且从未成功读取过时间：

```text
⚠ 当前聊天尚未获得有效故事时间。
请让 AI 输出完整 <wlog> 后再由插件接管时间线。
```

在此之前不修改世界书。

---

# 6. 自动运行触发时机

## 6.1 AI 回复完成后

插件只在 SillyTavern 确认本轮 AI 生成完成后读取最终消息。

流式生成过程中：

- 不解析 `<wlog>`
- 不更新时间
- 不操作世界书

如果用户中途点击停止生成：

- 最终消息中有完整合法 `<wlog>`：正常处理
- 没有合法 `<wlog>`：保持当前时间与世界书状态

---

## 6.2 打开 / 切换聊天

打开聊天时：

1. 优先读取 `chatMetadata.st_yafaya_timeline`
2. 如果已有合法保存时间，则直接使用保存时间恢复该聊天状态
3. 读取最新世界书公共配置
4. 对自动组重新计算当前应激活条目
5. 必要时重新同步世界书开关
6. **不主动重新读取最后一条 AI 正文来覆盖已有 chatMetadata 时间**

这一条是为了满足：

> 用户手动编辑 AI 正文中的 `<wlog>` 后，插件不得因此自动改变当前时间。

只有在 `chatMetadata` 缺失、损坏或从未初始化时，才读取最后一条 AI 回复中的 `<wlog>` 作为恢复 / 初始化来源。

---

## 6.3 不做生成前校验

V1 不在用户发送下一条消息前执行世界书状态保险校验。

也就是说：

- 不拦截生成
- 不在 generation start 前自动纠正世界书
- 自动同步仍以 AI 回复结束和聊天加载等既定时机为主

---

# 7. 手动获取当前时间

控制界面提供按钮：

> **获取当前时间**

该按钮专门用于：

- 用户手动修改 AI 正文后重新读取时间
- 用户希望强制重新同步当前 `<wlog>`
- 用户认为 chatMetadata 中保存时间已过期

执行规则：

1. 只读取当前聊天最后一条 AI 回复
2. 在该回复中从后向前寻找最后一个合法 `<wlog>`
3. 成功后更新当前时间
4. 重新计算自动时间线组
5. 如果时间倒退，进入倒退确认流程
6. 不搜索更早的 AI 消息

失败提示：

```text
⚠ 最后一条 AI 回复中未检测到有效时间。
请自行重 Roll 或补充时间。
```

失败时不改变当前世界书状态。

---

# 8. 时间倒退

例如：

```text
旧时间：424年5月14日
新时间：424年5月12日
```

自动模式不得直接应用。

显示：

```text
⚠ 检测到时间倒退
424年5月14日 → 424年5月12日

是否应用新时间？
```

用户确认后：

- 新时间成为当前时间
- 重新计算各自动组
- 必要时允许反向切换到更早时间线

用户拒绝或未确认：

- 保留原时间
- 保持原世界书开关

---

# 9. 大跨度时间跳跃

如果新时间明显晚于旧时间，例如：

```text
420/03/17 → 424/05/14
```

插件仍然直接使用新日期重新计算，不阻止自动切换。

如果跨度超过配置阈值：

```text
默认：365 天
```

显示轻量提醒：

```text
ℹ 检测到较大时间跳跃
420/03/17 → 424/05/14

已按新时间同步时间线。
```

不要求人工确认。

阈值可在设置中修改。

---

# 10. 时间线组模型

## 10.1 多组支持

一本世界书可以包含多个互不排斥的时间线组，例如：

```text
主线剧情
洛琪希个人线
世界局势
```

不同组可以同时各自启用一个时间线条目。

同一自动组内：

> 任意时刻只能有一个正确时间线条目处于启用状态。

---

## 10.2 AI 自动分组 + 人工修改

第一次扫描时：

- AI 自动判断哪些条目属于时间线
- AI 自动尝试分组
- AI 自动排序
- AI 自动生成组名
- 用户拥有最终修改权

允许：

- 改组名
- 新建组
- 拆分组
- 合并组
- 条目跨组移动
- 拖拽调整顺序

人工修改结果优先于 AI。

---

## 10.3 组名

第一次由 AI 自动生成简短组名。

例如：

```text
主线剧情
洛琪希个人线
世界局势
```

如果 AI 无法可靠命名：

```text
未命名时间线组 1
```

用户手动改名后，该字段进入人工锁定，后续重新分析不得自动覆盖。

---

# 11. 时间线条目显示模型

每个时间线卡片建议显示：

```text
米里希昂篇
419/11/10 ～ 419/12/25

原条目：世界线-17

● 生效中
自动
```

其中：

- `米里希昂篇`：AI 生成的插件展示标题
- 时间范围：插件整理 / 计算后的有效范围
- 原条目：SillyTavern 世界书中的真实名称
- 不修改原条目名称

AI 标题建议控制在：

> 约 4～12 个中文字符

偏篇章名风格。

用户手动标题不强制长度，UI 超长时省略显示。

---

# 12. 时间线边界规则

## 12.1 核心接壤逻辑

对于相邻条目：

> 当前条目的切换边界日期 = 下一条时间线的生效起点。

例如：

时间线①：

```text
最早事件：419/11/10
切换边界：419/12/26
```

时间线②内部最早明确事件：

```text
420/02/10
```

实际运行范围仍为：

```text
时间线①
419/11/10 ～ 419/12/25

时间线②
419/12/26 ～ 下一切换边界前一天
```

在：

```text
419/12/26
```

当天开始，立即：

```text
时间线① → 关闭
时间线② → 开启
```

不等待第二天。

---

## 12.2 第一条时间线

如果当前故事日期早于第一条时间线的实际内容起始日期：

> 仍然默认开启第一条。

但 UI 仍显示第一条真实内容范围，例如：

```text
419/11/10 ～ 419/12/25
```

不显示：

```text
-∞ ～ 419/12/25
```

“早于首条仍使用首条”属于运行规则，不属于展示范围。

---

## 12.3 最后一条时间线

如果当前已经到达最后一条，且不存在后续条目：

- 最后一条继续保持开启
- 不因为其内部存在“沙盒开始日期”就关闭
- 对应边界只作为“时间线已到末端”的提示依据

提示：

```text
已到达时间线末端，暂无后续条目
```

最后一条有效范围在 UI 可表示为：

```text
上一边界 ～ ∞
```

---

## 12.4 只按年月日判断

自动匹配忽略时分。

例如：

```text
424/05/14 00:01
424/05/14 17:15
424/05/14 23:59
```

都属于同一天。

---

# 13. 运行优化

## 13.1 同一天只更新时间

上一轮：

```text
424/05/14 17:15
```

下一轮：

```text
424/05/14 21:40
```

只更新完整时间记录。

不重新操作世界书。

不弹切换 Toast。

---

## 13.2 日期变化但仍命中同一条

例如：

```text
424/05/14 → 424/05/20
```

插件重新计算所属条目。

如果仍命中当前条目：

- 更新当前时间
- 不重新写世界书
- 不弹切换 Toast

---

## 13.3 只有条目发生变化才真正切换

运行逻辑：

```text
日期变化
↓
重新计算目标条目
↓
目标条目是否变化？
├─ 否 → 只更新时间
└─ 是 → 执行世界书切换
```

---

## 13.4 跨多个阶段直接跳转

如果故事时间一次跨越多个时间线：

```text
时间线① → 时间线④
```

插件不逐级执行：

```text
① → ② → ③ → ④
```

而是直接根据新日期计算当前唯一正确条目：

```text
→ ④
```

然后一次性同步组内开关。

---

# 14. 自动模式与手动模式

## 14.1 每组独立模式

每个时间线组拥有：

```text
自动
手动
```

V1 不提供插件级“暂停全部自动控制”。

---

## 14.2 自动模式

自动模式下：

- 插件拥有组内受管条目的开关控制权
- 开启当前正确条目
- 关闭组内其他时间线条目
- 不尊重这些条目进入自动模式前的旧开关状态

卡片中的开关：

- 显示真实状态
- 不允许点击
- 标记“由自动模式控制”

---

## 14.3 手动模式

手动模式下插件完全停止操作该组。

用户可以：

- 开启多个条目
- 关闭多个条目
- 全部关闭
- 任意组合

不要求单选。

---

## 14.4 自动 → 手动

切换瞬间：

> 保持当前世界书开关状态原样不动。

插件从此停止干预该组。

---

## 14.5 手动 → 自动

立即使用当前聊天已经保存的有效时间重新计算：

- 关闭错误条目
- 开启正确条目

如果当前没有有效时间，则不操作并提示。

---

# 15. 外部手动修改

如果某组处于自动模式，而用户在 SillyTavern 原生世界书界面修改了组内条目开关：

插件不会即时拦截。

下一次正常自动同步时：

- 根据当前时间重新计算
- 恢复正确开关状态

运行日志记录：

```text
检测到外部修改，已重新同步自动时间线状态。
```

如果用户希望保留手工状态，应先切换该组为手动模式。

---

# 16. AI 时间线识别

## 16.1 AI 的职责

AI 只负责配置阶段的：

- 判断哪些世界书条目属于时间线
- 自动分组
- 自动排序
- 生成组名
- 生成展示标题
- 解析条目内日期
- 判断时间边界
- 检测时间异常
- 缺失边界时提出候选
- 发现冲突或低置信度项目

AI 不负责运行阶段的 `<wlog>` 日期判断。

---

## 16.2 规则优先，AI 兜底

世界书时间线分析时：

1. 插件优先用本地规则解析明确日期 / 结构
2. 无法可靠解析的内容再交给 AI
3. AI 推导结果仍需经过 Schema 校验
4. 低置信度、缺失边界、冲突等情况需要人工确认

`沙盒模式` 不作为唯一写死关键词。

插件关心的是：

> 最终是否能得到可靠且经确认的时间边界。

---

# 17. 世界书扫描策略

## 17.1 快速扫描

默认先本地筛选候选条目。

候选特征可以包括：

- 包含 `<world_timeline>`
- 日期密度较高
- 大量“事件 + 日期”结构
- 其他明显时间线文本特征

然后只把候选交给 AI。

---

## 17.2 深度扫描全部条目

界面提供：

> **深度扫描全部条目**

用于处理格式特殊、被本地规则漏掉的时间线。

---

## 17.3 变化检测

对于已经建立配置的世界书：

- 内容未变化：不重复调用 AI
- 新增 / 删除 / 修改：标记为待检查
- 可提供“自动分析变更”开关
- 用户始终可以手动点击“重新分析”

即使自动分析变更开启：

> AI 只能生成新建议，不能直接覆盖旧配置。

---

# 18. AI 批处理策略

小规模候选：

> 一次请求完成。

大规模候选：

```text
候选条目
↓
自动分批
↓
每批分析
↓
生成精简结构化结果
↓
最终汇总请求
↓
统一分组 / 排序 / 命名 / 冲突检查
```

分批数量由实现根据上下文规模决定。

V1 可采用约 20～30 条一批的思路，但不要求写死。

---

# 19. AI 输入字段

默认只发送：

```text
entryId
comment / 原条目名称
content / 条目正文
```

不默认发送：

- Keys
- Secondary Keys
- Order
- Depth
- Position
- Probability
- Strategy
- 其他与时间线分析无直接关系的字段

---

# 20. AI 输出格式

AI 必须返回结构化 JSON。

示例：

```json
{
  "groups": [
    {
      "name": "主线剧情",
      "entries": [
        {
          "entryId": 123,
          "title": "米里希昂篇",
          "contentStartDate": "419-11-10",
          "boundaryDate": "419-12-26",
          "confidence": 0.98,
          "warnings": []
        }
      ]
    }
  ]
}
```

插件必须在展示给用户前做 Schema 校验。

AI 的自然语言说明：

> 不得直接驱动世界书开关。

---

# 21. AI 输出校验与重试

如果 AI 返回：

- 非法 JSON
- 缺失字段
- 日期不合法
- 引用了不存在的条目 ID
- Schema 不匹配

则自动修复 / 重试。

最多：

> **3 次**

连续 3 次失败后停止继续消耗 Token。

提示：

```text
⚠ AI 分析结果连续 3 次无法通过结构校验
```

提供：

- 重新分析
- 更换模型
- 查看原始输出

已有旧配置保持不变。

---

# 22. AI 置信度

识别结果分三级：

## 高置信度

进入 AI 建议结果。

## 中置信度

标记：

```text
待确认
```

## 低置信度

默认不勾选，只显示：

```text
可能相关
```

未经人工确认的低置信度条目：

> 不得参与自动开关。

---

# 23. 缺失时间边界

如果某条时间线无法可靠得到结束 / 切换边界：

AI 可以提出候选。

例如：

```text
AI 推导边界：420年3月31日
状态：⚠ 待确认
```

用户可以：

- 确认
- 修改日期
- 忽略
- 移出时间线

只有人工确认后的边界才能参与自动切换。

---

# 24. 日期异常检测

AI 扫描时允许提示：

- 日期倒序
- 疑似月份笔误
- 时间链异常
- 同一事件链时间冲突
- 不合理大跳跃

例如：

```text
B1：419年11月11日
B2：419年1月11日
B3：419年1月11日
```

显示：

```text
⚠ 疑似日期异常，请检查。
```

只提示：

- 不修改原世界书
- 不自动修正
- 不自动把 AI 猜测写入正式运行配置

---

# 25. 扫描结果确认页

第一次扫描完成后：

> AI 结果只是草稿。

必须进入“扫描结果确认页”。

用户可以：

- 查看分组
- 查看条目排序
- 修改 AI 标题
- 修改时间范围
- 修改组名
- 新建组
- 合并组
- 拆分组
- 跨组移动条目
- 拖拽排序
- 勾选 / 取消纳入自动控制
- 确认 AI 推导边界
- 处理时间异常
- 处理冲突
- 查看置信度
- 查看原世界书正文

只有点击：

> **确认并应用**

配置才正式保存。

---

# 26. 第一次确认后的运行

第一次点击“确认并应用”后：

- 时间线组默认进入自动模式
- 如果当前聊天已有合法时间，立即根据该时间同步正确条目
- 如果当前聊天尚未出现过合法 `<wlog>`，则暂不操作世界书
- 等第一次合法 `<wlog>` 出现后正式接管

---

# 27. 人工修改优先级

用户手动修改过的字段进入人工锁定。

包括但不限于：

- 组名
- 展示标题
- 日期
- 边界
- 分组
- 顺序

后续 AI 重新分析：

> 不得直接覆盖人工锁定字段。

---

# 28. 原世界书正文预览

时间线卡片支持：

> **查看正文**

正文严格只读。

默认显示前约：

> 500～1000 字

并提供：

- 展开全文
- 收起
- 复制正文

插件不提供在卡片内直接编辑原世界书正文的能力。

---

# 29. 搜索与筛选

时间线管理页 V1 必须支持：

- 按 AI 展示标题搜索
- 按原世界书条目名称搜索
- 按时间线组筛选
- 只看当前生效
- 只看异常 / 待确认
- 只看人工修改过

V1 不做复杂表达式查询。

---

# 30. 时间线冲突

如果同一组出现：

- 时间范围重叠
- 同日起始
- 多个条目同时可匹配
- 顺序无法唯一确定
- 手工配置产生空档

该组进入阻塞状态。

处理：

- 不修改该组世界书开关
- 显示冲突
- 要求人工处理

用户可以：

- 修改日期
- 调整顺序
- 移出条目
- 修改分组
- 重新确认正确项

冲突解决后自动模式才能重新执行。

---

# 31. 当前日期无法匹配

如果 `<wlog>` 成功解析出：

```text
424年5月14日
```

但当前时间线配置中没有唯一有效匹配：

```text
⚠ 当前时间 424年5月14日 未匹配到有效时间线。
请检查时间线配置。
```

处理：

- 不修改任何条目
- 不选择“最近时间线”
- 不调用 AI 猜测
- 提供“前往检查”

该错误只影响当前有问题的时间线组。

---

# 32. 时间线组故障隔离

不同时间线组故障互不连坐。

例如：

```text
主线剧情       ● 正常
洛琪希个人线   ● 正常
世界局势       ⚠ 配置冲突
```

只有“世界局势”停止本轮自动操作。

其他组继续正常运行。

---

# 33. 世界书切换执行与校验

真正发生切换时：

```text
计算目标状态
↓
只修改当前组的受管条目启用状态
↓
重新读取实际状态
↓
校验是否符合预期
```

如果第一次失败：

> 自动重试 1 次。

仍失败：

```text
⚠ 时间线切换失败：主线剧情
预期：西部港篇
实际：米里希昂篇仍开启
```

提供：

> **重新同步**

任何情况下不得修改非受管世界书条目。

---

# 34. 切换 Toast

真正发生条目变化后，默认显示短暂 Toast：

```text
🕒 时间线已切换
米里希昂篇 → 西部港篇
```

Toast：

- 不写入聊天正文
- 可在设置中关闭
- 同一条目内日期变化不显示

---

# 35. 聊天运行日志

每个聊天保存最近：

> **100 条**

示例：

```text
424/05/14 17:15
主线剧情
夏利亚篇 → 环球探索篇
原因：跨越时间边界
结果：成功
```

日志可记录：

- 时间
- 组名
- 原条目
- 新条目
- 触发原因
- 执行结果
- 是否重试
- 外部修改重新同步
- 大跨度跳跃

不保存：

- API Key
- 完整聊天正文
- 完整世界书正文

提供：

- 运行日志页
- 清空日志

---

# 36. 插件系统日志

系统日志与聊天运行日志分开。

记录：

- AI 扫描
- AI JSON 校验失败
- API 测试
- 配置导入 / 导出
- 世界书重新分析
- 配置迁移 / 重新匹配动作
- 插件运行错误

不混入剧情时间线切换日志。

---

# 37. 世界书正文变化

已经确认并正在运行的时间线条目如果正文 Hash 变化：

> **旧配置继续生效。**

显示：

```text
⚠ 世界书正文已修改，时间线配置可能已过期
```

提供：

> **重新分析**

正文变化不得直接改变当前时间线边界。

---

# 38. 重新分析变更确认

AI 重新分析后，不直接覆盖旧配置。

显示对比：

```text
原配置：
419/11/10 ～ 419/12/25

AI 新建议：
419/11/10 ～ 419/12/29
```

用户选择：

- 采用新结果
- 保留原配置
- 手动修改

在确认之前：

> 旧配置继续用于自动运行。

---

# 39. 条目身份识别

为应对导出 / 重导导致条目 ID 改变，匹配依据包括：

```text
原条目 ID
+
内容 Hash
+
原条目名称
+
内容相似度
```

优先级：

1. ID 一致
2. Hash 一致
3. 名称 + 内容相似度
4. 无法确认 → 作为新条目

疑似原条目时：

```text
检测到疑似原时间线条目，是否继承旧配置？
```

---

# 40. 原条目被删除

如果已纳管时间线对应的世界书条目消失：

不立即删除插件配置。

显示：

```text
⚠ 原条目已不存在
```

提供：

- 重新匹配
- 从时间线移除
- 删除旧配置

---

# 41. 世界书改名

如果检测到世界书可能发生重命名：

```text
检测到世界书可能已更名
```

用户选择：

- 迁移原配置
- 作为新世界书

不得只凭名称自动迁移。

---

# 42. 世界书解绑 / 丢失

当前角色不再绑定原受管世界书：

- 立即停止该书自动控制
- 保留聊天状态
- 不删除配置
- 不自动重新挂载世界书

提示：

```text
⚠ 当前角色绑定的世界书不存在或无法读取。
时间线自动控制已暂停。
```

重新绑定后：

- 读取聊天保存时间
- 读取最新世界书配置
- 自动组重新计算并恢复

---

# 43. 切换角色卡

切换角色卡时：

1. 立即停止操作旧角色世界书
2. 读取新角色实际绑定世界书
3. 如果新世界书已有时间线配置，加载配置
4. 如果从未整理过：

```text
当前世界书尚未建立时间线配置
```

提供：

> **开始扫描**

切角色本身不会自动调用 AI。

---

# 44. 新聊天初始化

如果世界书已经整理好，但当前聊天没有时间线 chatMetadata：

1. 加载世界书公共时间线配置
2. 等待第一次合法 `<wlog>`
3. 获得时间后建立聊天运行状态
4. 自动组开始接管

在第一次合法 `<wlog>` 出现之前：

> 不修改世界书开关。

---

# 45. ChatMetadata 缺失或损坏

如果聊天运行状态缺失或损坏：

1. 尝试读取当前聊天最后一条 AI 回复
2. 解析其中最后一个合法 `<wlog>`
3. 结合当前世界书公共配置重建当前状态

可以重建：

- 当前时间
- 当前应激活条目

不会伪造：

- 已丢失运行日志
- 旧待确认倒退状态
- 其他不可恢复历史

---

# 46. 同一本世界书的多聊天状态

同一本角色世界书可以有多个聊天，各自保存：

- 当前时间
- 当前激活条目
- 各组自动 / 手动状态
- 异常状态
- 运行日志

切换到另一个聊天时：

> 使用该聊天保存时间 + 最新世界书配置重新计算自动组。

因此世界书公共时间线配置修改后：

- 当前聊天立即重算
- 其他未打开聊天不后台遍历
- 以后进入这些聊天时再按新配置重算

---

# 47. 多标签页控制权

同一本受管世界书不能同时被多个浏览器标签页以不同聊天自动控制。

采用：

> **单控制权机制**

如果检测到其他标签页已经控制同一本世界书：

```text
⚠ 检测到同一本世界书正在其他标签页由时间线插件控制。
为避免状态冲突，本标签页自动控制已暂停。
```

提供：

> **接管控制权**

实现建议：

- `BroadcastChannel`
- 配合短期 heartbeat / 锁记录
- 锁仅用于运行期协调，不作为持久配置

---

# 48. 配置修改后的即时重算

用户修改已确认时间线配置并保存：

## 当前组为自动模式

立即使用当前聊天保存时间重新计算。

如果目标条目变化：

- 立即切换

如果新配置产生冲突 / 空档：

- 不修改世界书
- 显示错误

## 当前组为手动模式

只保存配置。

不操作世界书。

---

# 49. AI 接入模式

支持两种方式。

## 49.1 跟随 SillyTavern 当前 API

默认零配置模式。

AI 分析通过 SillyTavern 当前可用模型能力发送分析请求。

具体调用方式以当前实际运行环境中可用的 SillyTavern API 为准。

---

## 49.2 独立 OpenAI Compatible API

支持配置：

- API URL
- API Key
- 模型名称
- Temperature
- 最大输出 Token
- 请求超时

V1 不额外实现：

- Claude 原生协议
- Gemini 原生协议
- 其他厂商专用协议

只要有 OpenAI Compatible 端点即可。

---

# 50. API Key 安全

API Key：

- 保存在插件全局设置
- UI 默认遮挡
- 支持显示 / 隐藏
- 提供“测试连接”
- 配置导出不包含 Key
- 日志不打印完整 Key
- 错误信息不得泄露完整 Key

---

# 51. 扫描进度 UI

AI 扫描必须在界面显示实时进度。

例如：

```text
正在筛选候选条目 128 / 500
AI 分析第 2 / 5 批
正在汇总分组结果
正在校验 JSON
```

提供：

> **取消扫描**

取消后：

- 尽可能中止当前 AI 请求
- 不保存半成品
- 不覆盖旧配置
- 可重新发起扫描

---

# 52. 关闭弹窗时扫描继续

如果用户关闭“时间线管理”大弹窗：

> 扫描继续运行。

重新打开弹窗：

> 恢复显示当前扫描进度。

只有明确点击“取消扫描”才终止。

---

# 53. 页面刷新时扫描中断

如果：

- 刷新 SillyTavern
- 关闭标签页
- 页面崩溃

未完成扫描视为中断。

重新打开后：

```text
上一次扫描未完成，请重新扫描。
```

不做：

- 扫描断点恢复
- 自动续跑
- 半成品落盘

旧配置不受影响。

---

# 54. 配置导入 / 导出

支持当前世界书时间线配置：

- 导出 JSON
- 导入 JSON

导出内容包括：

- 分组
- 条目映射
- 标题
- 日期
- 人工修正
- 确认状态
- 必要配置标识

绝不包含 API Key。

导入时：

1. 校验世界书
2. 校验条目 ID
3. 校验 Hash
4. 检查缺失条目
5. 尝试重新匹配
6. 展示差异确认

导入配置不得直接无确认接管世界书。

---

# 55. UI 入口

入口位于：

> **SillyTavern 左下角魔法棒菜单**

菜单项：

```text
[fa-timeline] 时间线管理
```

不在消息右侧按钮区域放入口。

点击后打开：

> **大弹窗**

---

# 56. UI 信息架构

推荐导航：

```text
总览
时间线
分组
AI分析
日志
设置
```

---

# 57. 总览页

第一眼应看到：

- 当前角色
- 当前绑定世界书
- 当前故事时间
- 当前时间线组
- 每组自动 / 手动状态
- 每组当前激活条目
- 当前警告 / 错误
- 获取当前时间
- 重新扫描 / 分析入口

示例：

```text
当前时间
424年5月14日 17:15

主线剧情
● 自动
环球探索篇

洛琪希个人线
● 自动
中央大陆旅行篇

世界局势
⚠ 配置异常
```

---

# 58. 时间线管理页

采用分类 + 卡片结构。

每组可：

- 展开 / 收起
- 切换自动 / 手动
- 查看当前激活条目
- 编辑组名
- 查看异常

每条卡片可：

- 查看展示标题
- 查看真实时间范围
- 查看原条目名称
- 查看生效状态
- 查看置信度 / 警告
- 查看正文
- 在手动模式下控制开关
- 在配置编辑状态下调整日期 / 标题 / 分组 / 顺序

---

# 59. UI 视觉风格

整体目标：

- 大留白
- 圆角卡片
- 分类标题
- 清晰层级
- 低视觉噪声
- 柔和状态提示
- 当前生效项明显但不过度刺眼
- 错误 / 待确认状态容易发现

不做传统“密密麻麻表单式后台”。

---

# 60. 主题

支持：

```text
跟随 SillyTavern
浅色
深色
```

默认：

> **跟随 SillyTavern**

主题设置只保存以下偏好值：

```text
follow | light | dark
```

`follow` 不是第三套主题。插件运行时读取 SillyTavern 当前主题背景的实际亮度，将其映射为
`light` 或 `dark`，并监听宿主主题变化实时同步；用户选择浅色或深色后则固定使用对应主题。
主题 Listbox 的选择必须立即应用并保存，不得要求用户再次点击常规设置保存按钮。

插件最外层只输出 `data-theme="light"` 或 `data-theme="dark"`。组件结构保持不变，所有背景、卡片、
文字、边框、Primary、Hover、Listbox、滚动条、Modal 和 Toast 的颜色必须通过语义化 CSS Variables
取值，例如 `var(--tl-bg)`、`var(--tl-surface)`、`var(--tl-text)`、`var(--tl-border)` 与
`var(--tl-primary)`。组件样式内不得写死颜色；当前冻结 UI 是正式 Dark Token，Light Token 只替换颜色，
不得改变组件几何、布局和交互结构。

插件仍保持自己的：

- 布局
- 间距
- 圆角
- 卡片结构
- 交互状态

只跟随基础主题颜色。

---

# 61. Shadow DOM

推荐把插件主 UI 渲染在 Shadow DOM 内：

- 防止 SillyTavern 第三方主题污染插件
- 防止插件 CSS 反向污染 SillyTavern
- 大弹窗使用 fixed 定位
- 大弹窗外层尺寸固定，不得因 Tab 内容多少发生伸缩；顶部导航和上下文栏固定，只有中间内容区独立滚动
- Tab 的 Normal / Hover / Active / Focus 状态必须冻结几何尺寸：宽高、内边距、边框宽度、字号、字重和行高均不得变化；只允许改变文字色、背景、边框色、阴影 / 焦点环或绝对定位伪元素
- Tab 切换仅让背景、文字颜色和底部指示条以 150～200ms `ease-out` 过渡；新内容区只允许 `opacity` 与 `translateY(4px)` 淡入，禁止对宽高、padding、font-weight 或任何布局尺寸做动画
- 时间线页与设置页不得使用原生 `<select>`；统一使用 Token 驱动的自定义 Listbox，弹层绝对定位且不撑开父容器，采用 Timeline Blue 选中态、低对比 Hover、圆角、Shadow，以及 150～180ms `opacity + translateY(4px)` 动画
- 主内容区和 Listbox 选项层统一使用细窄的 Token 驱动滚动条：轨道透明或低对比，滑块为低饱和蓝灰且 Hover 时轻微提亮；使用 `scrollbar-gutter: stable` 防止横向跳动，不得回退为不受主题控制的系统滚动条
- 设置页仍是固定 Shell、顶部主导航和状态栏之下的六个主 View 之一，只重构 Main Content；不得增加设置专用 Header、底部 Tab Bar、独立滚动 Shell 或任何会改变外层 Modal 几何尺寸的结构。
- 设置 Main Content 使用居中的单列 Accordion，不再使用左侧分类栏与右侧详情面板。常规、AI 分析、自动切换、数据管理默认折叠，只显示标题、副标题和展开箭头；展开动画限制在 150～180ms，变化只能发生在内容区内部。
- 设置卡片使用不透明语义 Token；Light Token 下呈白色或极浅灰卡片、弱边框、16～20px 圆角与轻阴影，不得使用毛玻璃或大面积高对比色块。
- AI 分析折叠组按接口提供商、输入项、连接状态、操作按钮的顺序纵向排列，保留 SillyTavern API、独立 API、模型、连接测试和全部高级参数。
- 避免将需要主题变量的内容 Teleport 到 Shadow DOM 外部

---

# 62. PC 与移动端

桌面端和移动端都属于正式支持目标。

## PC

- 大弹窗
- 多列卡片
- 拖拽排序
- 完整导航

## 手机 / 平板

- 接近全屏
- 单列卡片
- 触屏拖拽
- 不依赖 hover
- 所有核心功能可操作

移动端必须支持：

- 扫描
- 进度
- 扫描确认
- 分组
- 排序
- 自动 / 手动
- 搜索筛选
- 日志
- AI 设置
- 异常处理
- 导入 / 导出

---

# 63. V1 不做的功能

V1 明确不做：

- 插件级“暂停全部自动控制”
- 生成前世界书保险校验
- “撤销上一次自动切换”
- Dry Run / 预演模式
- 复制时间线组
- 独立自动更新器
- 配置 Schema 自动迁移系统
- 国际化 / 多语言
- 旧版 SillyTavern 兼容包袱
- Claude / Gemini 厂商专用 API 协议
- 跨世界书时间线组
- 自动修改世界书正文
- 运行阶段 AI 猜当前日期

---

# 64. 插件更新方式

通过 SillyTavern 第三方扩展常规机制：

- GitHub 仓库安装
- `manifest.json`
- SillyTavern 扩展管理器更新

插件自身不开发：

- 后台 GitHub 版本检查
- 自动更新器

---

# 65. SillyTavern 版本策略

V1 不声明最低 SillyTavern 客户端版本，扩展清单不使用 `minimum_client_version` 限制安装或加载。

开发时需要针对当前实际运行环境验证：

- `getContext()`
- 当前角色读取
- 角色绑定世界书读取
- 世界书条目列表读取
- 条目启用 / 禁用写入
- 世界书保存
- AI 回复完成事件
- 聊天切换事件
- `extension_settings`
- `chatMetadata`
- 魔法棒菜单
- 当前 AI 调用能力

若某版本出现 API 缺失或行为差异：

> 记录可复现问题并针对实际故障修复；在问题出现前不为未知旧版或新版预先堆兼容层。

---

# 66. 推荐模块划分

以下为推荐实现结构，不要求目录名完全一致：

```text
ST-yafaya-Timeline/
├── manifest.json
├── package.json
├── src/
│   ├── index.ts
│   │
│   ├── st/
│   │   ├── context.ts
│   │   ├── events.ts
│   │   ├── characterWorldbook.ts
│   │   └── worldInfoAdapter.ts
│   │
│   ├── timeline/
│   │   ├── types.ts
│   │   ├── date.ts
│   │   ├── wlog.ts
│   │   ├── matcher.ts
│   │   ├── engine.ts
│   │   ├── switcher.ts
│   │   ├── validator.ts
│   │   └── identity.ts
│   │
│   ├── ai/
│   │   ├── client.ts
│   │   ├── prompts.ts
│   │   ├── schema.ts
│   │   ├── scan.ts
│   │   └── batch.ts
│   │
│   ├── storage/
│   │   ├── global.ts
│   │   ├── worldbook.ts
│   │   └── chat.ts
│   │
│   ├── lock/
│   │   └── tabControl.ts
│   │
│   ├── ui/
│   │   ├── App.vue
│   │   ├── pages/
│   │   ├── components/
│   │   └── state/
│   │
│   ├── logs/
│   │   ├── runtime.ts
│   │   └── system.ts
│   │
│   └── styles/
│       ├── base.css
│       └── theme.css
└── dist/
```

---

# 67. 推荐核心数据结构

示意 TypeScript：

```ts
type GroupMode = 'auto' | 'manual';

interface StoryTime {
  year: number;
  month: number;
  day: number;
  hour?: number;
  minute?: number;
  raw: string;
}

interface ManagedEntry {
  entryId: string | number;
  originalComment: string;
  contentHash: string;

  displayTitle: string;
  titleLocked: boolean;

  contentStartDate?: string;
  effectiveStartDate?: string;
  boundaryDate?: string;
  effectiveEndDate?: string | null;

  confidence: number;
  warnings: string[];

  managed: boolean;
  manualFields: string[];
  stale: boolean;
}

interface TimelineGroup {
  id: string;
  name: string;
  nameLocked: boolean;
  entries: ManagedEntry[];
  blocked: boolean;
  blockReason?: string;
}

interface WorldbookTimelineConfig {
  worldbookKey: string;
  worldbookName: string;
  groups: TimelineGroup[];
  updatedAt: number;
}

interface ChatGroupState {
  mode: GroupMode;
  activeEntryId?: string | number;
}

interface ChatTimelineState {
  currentTime?: StoryTime;
  groups: Record<string, ChatGroupState>;
  pendingRollback?: {
    from: StoryTime;
    to: StoryTime;
  };
  logs: RuntimeLog[];
}
```

---

# 68. WorldInfo Adapter 约束

不要让业务逻辑直接依赖 SillyTavern 内部世界书实现。

推荐封装：

```ts
interface WorldInfoAdapter {
  getCurrentCharacterWorldbook(): Promise<WorldbookRef | null>;
  listEntries(worldbook: WorldbookRef): Promise<WorldInfoEntry[]>;
  getEntryEnabled(worldbook: WorldbookRef, entryId: string | number): Promise<boolean>;
  setEntryEnabled(
    worldbook: WorldbookRef,
    entryId: string | number,
    enabled: boolean
  ): Promise<void>;
  saveWorldbook(worldbook: WorldbookRef): Promise<void>;
}
```

业务层只能通过该适配器修改：

> `enabled`

不得暴露“修改正文”等写接口给自动切换引擎。

---

# 69. `<wlog>` 解析器约束

建议独立实现纯函数：

```ts
parseWlogTime(messageText: string): StoryTime | null
```

要求：

- 只解析当前消息文本
- 支持一条消息多个 `<wlog>`
- 从后往前寻找最后合法项
- 不调用 AI
- 不读取更早聊天历史
- 不抛出导致主流程崩溃的异常

---

# 70. 时间线匹配器约束

建议：

```ts
matchTimelineEntry(
  date: StoryDate,
  group: TimelineGroup
): MatchResult
```

MatchResult 至少区分：

```ts
matched
no_match
conflict
invalid_config
```

匹配器必须是纯规则逻辑。

不得：

- 调 AI
- 自动修正日期
- 选择最近条目
- 静默吞掉冲突

---

# 71. 自动切换流程

```text
AI 生成结束
        ↓
读取最终 AI 消息
        ↓
解析最后一个合法 <wlog>
        ↓
失败？
 ┌──────┴──────┐
 是             否
 ↓              ↓
保持当前状态    与当前保存时间比较
提示用户             ↓
                是否倒退？
                ↓
           是 → 等待人工确认
                ↓
               否
                ↓
             保存时间
                ↓
        年月日是否发生变化？
        ┌───────┴───────┐
        否               是
        ↓                ↓
   只更新时间       对每个自动组重新匹配
                         ↓
                    组是否可匹配？
                   ┌─────┴─────┐
                   否           是
                   ↓            ↓
              仅该组报错     目标是否变化？
                            ┌───┴───┐
                            否       是
                            ↓        ↓
                       不写世界书   执行切换
                                      ↓
                                   读取校验
                                      ↓
                                  失败重试一次
                                      ↓
                                 Toast + 日志
```

---

# 72. 世界书配置编辑后的流程

```text
用户修改时间线配置
        ↓
点击保存 / 确认
        ↓
更新世界书公共配置
        ↓
当前组是否自动？
   ┌────┴────┐
   否         是
   ↓          ↓
只保存      使用当前 chatMetadata 时间重算
              ↓
         配置是否有效？
         ┌────┴────┐
         否         是
         ↓          ↓
      报错不切    必要时切换
```

---

# 73. 扫描流程

```text
点击开始扫描
      ↓
读取当前角色绑定世界书
      ↓
读取全部条目 ID / comment / content
      ↓
快速扫描？
 ┌────┴────┐
 是         否（深度）
 ↓          ↓
本地筛候选   全部作为候选
      ↓
根据规模决定一次 / 分批
      ↓
AI 结构化分析
      ↓
Schema 校验
      ↓
失败最多重试 3 次
      ↓
批次汇总
      ↓
冲突 / 异常 / 置信度检查
      ↓
扫描结果确认页
      ↓
用户人工校正
      ↓
确认并应用
      ↓
保存世界书公共配置
      ↓
当前聊天有有效时间？
 ┌────┴────┐
 否         是
 ↓          ↓
等待 wlog   自动组立即同步
```

---

# 74. V1 验收标准

V1 只有在以下核心功能全部通过实际测试后才视为完成。

## 74.1 扩展与 UI

- [ ] 可通过 GitHub 安装为 SillyTavern 第三方扩展
- [ ] 当前实际运行的 SillyTavern 正常加载
- [ ] `manifest.json` 不声明最低客户端版本
- [ ] 魔法棒菜单出现 `时间线管理`
- [ ] 点击后打开大弹窗
- [ ] 中文 UI 完整
- [ ] 跟随 ST / 浅色 / 深色主题正常
- [ ] PC 正常使用
- [ ] 手机 / 平板核心流程完整可用

## 74.2 世界书识别

- [ ] 正确读取当前角色卡实际绑定世界书
- [ ] 无绑定世界书时不误操作其他世界书
- [ ] 只读取当前角色世界书
- [ ] 非受管条目永不被自动修改

## 74.3 AI 扫描

- [ ] 快速扫描可筛候选
- [ ] 深度扫描可分析全部条目
- [ ] 小世界书单次分析正常
- [ ] 大世界书分批 + 汇总正常
- [ ] 扫描进度可视
- [ ] 扫描可取消
- [ ] 关闭弹窗扫描继续
- [ ] 页面刷新时扫描安全中断
- [ ] AI 输出使用结构化 JSON
- [ ] Schema 校验正常
- [ ] 错误最多自动重试 3 次
- [ ] 高 / 中 / 低置信度显示正常

## 74.4 扫描确认

- [ ] 可改标题
- [ ] 可改组名
- [ ] 可改日期
- [ ] 可拖拽排序
- [ ] 可跨组移动
- [ ] 可新建 / 合并 / 拆分组
- [ ] 可处理低置信度
- [ ] 可确认 AI 推导边界
- [ ] 可查看原正文
- [ ] 确认前不会接管世界书

## 74.5 `<wlog>` 时间解析

- [ ] 能解析标准 `<wlog time>`
- [ ] 一条消息多个 `<wlog>` 时取最后合法项
- [ ] 只按年月日参与切换
- [ ] 同一天仅更新时间
- [ ] 缺失 `<wlog>` 时不改世界书
- [ ] 新聊天没有时间时不接管
- [ ] “获取当前时间”按钮工作正常
- [ ] 手动编辑正文不会自动触发时间变化
- [ ] 时间倒退要求人工确认
- [ ] 大跨度前进能正常切换并提醒

## 74.6 时间线运行

- [ ] 早于第一条时默认使用第一条
- [ ] 达到切换边界当天立即进入下一条
- [ ] 可一次跨多个阶段直接命中正确条目
- [ ] 日期变化但同条目时不重复写世界书
- [ ] 最后一条无后续时保持开启
- [ ] 时间空档时不猜测
- [ ] 时间冲突时不猜测
- [ ] 多组独立运行
- [ ] 某一组故障不影响其他组

## 74.7 自动 / 手动

- [ ] 每组可独立自动 / 手动
- [ ] 自动模式保证唯一正确条目
- [ ] 手动模式允许任意开关组合
- [ ] 自动 → 手动保持现状
- [ ] 手动 → 自动立即按当前时间校正
- [ ] 自动模式可纠正外部手动改动

## 74.8 世界书写入安全

- [ ] 自动引擎只修改受管条目 enabled 状态
- [ ] 条目正文不被修改
- [ ] Comment 不被修改
- [ ] Key / Depth / Order 等不被修改
- [ ] 切换后会重新读取状态校验
- [ ] 失败自动重试一次
- [ ] 二次失败会报错而不是继续乱改

## 74.9 持久化

- [ ] 全局设置通过 `extension_settings` 保存
- [ ] 世界书整理结果持久化
- [ ] 聊天运行状态通过 `chatMetadata` 保存
- [ ] 同一本世界书不同聊天状态独立
- [ ] 切换聊天能恢复正确状态
- [ ] ChatMetadata 丢失时可从最后一条 AI `<wlog>` 重建当前状态

## 74.10 世界书变化

- [ ] 世界书正文变化能通过 Hash 检测
- [ ] 正文变化时旧配置继续运行
- [ ] 重新分析不会直接覆盖旧配置
- [ ] 可查看旧配置 / AI 新建议差异
- [ ] 条目 ID 改变时可通过 Hash / 相似度重新匹配
- [ ] 条目删除后旧配置不会立即丢失
- [ ] 世界书疑似改名时询问用户

## 74.11 多标签页

- [ ] 同一本世界书只有一个标签页拥有自动控制权
- [ ] 其他标签页不会偷偷修改世界书
- [ ] 可手动接管控制权

## 74.12 日志与导入导出

- [ ] 每聊天最近 100 条运行日志正常
- [ ] 插件系统日志独立
- [ ] 世界书时间线配置可导出 JSON
- [ ] JSON 导出不含 API Key
- [ ] JSON 导入先校验 / 匹配 / 确认
- [ ] 导入不会未经确认直接接管世界书

---

# 75. V1 完成定义

ST-yafaya-Timeline V1 的核心目标不是做一个通用世界书编辑器，而是完成一条可靠的闭环：

```text
读取角色卡世界书
        ↓
AI 整理时间线
        ↓
用户确认
        ↓
读取 <wlog> 当前日期
        ↓
确定性匹配正确条目
        ↓
安全切换世界书启用状态
        ↓
异常时停止猜测并交给用户
```

最终产品必须做到：

> **配置阶段智能，运行阶段确定；自动化足够省事，但不会为了“自动”而擅自猜测或破坏原世界书。**

这条原则是 V1 所有实现决策的最高优先级。
