# YaKit-理脉 Q&A

本文面向普通 SillyTavern 用户，按 YaKit-理脉 `0.2.3` 的实际行为编写。

[返回 readme.md](readme.md)

## 入口与扫描

### 1. 找不到 YaKit-理脉入口怎么办？

确认扩展已经在 SillyTavern 的第三方扩展列表中加载，然后打开左下角魔法棒菜单，入口名称是 **YaKit-理脉**，图标为时间线图标。`YaKit-Timeline` 是技术名称，不是菜单显示名。

如果入口仍不存在：

1. 在扩展管理器确认仓库安装成功。
2. 刷新 SillyTavern 页面后再次打开魔法棒菜单。
3. 仍无入口时，卸载后用仓库地址重新安装：<https://github.com/git-yafaya/ST-YaKit-Timeline.git>。

### 2. 显示“未绑定世界书”怎么办？

插件只读取当前角色卡实际绑定的世界书，不会根据角色名、世界书名称或全局启用项猜测目标。

请在当前角色卡的世界书设置中绑定目标世界书并保存，然后关闭并重新打开 YaKit-理脉，或切换一次聊天让上下文重新读取。未绑定期间插件不会扫描，也不会修改任何世界书。

### 3. “开始扫描”或扫描按钮不可用怎么办？

扫描按钮只有在以下条件同时满足时才可用：

- 当前角色已经读取成功；
- 当前角色实际绑定了一本可读取的世界书；
- 世界书中至少有一个条目。

绑定世界书或新增条目后，重新打开插件。若快速扫描可用而深度扫描失败，通常是 AI 接口配置问题；若两个按钮都不可用，请先处理当前角色、世界书或条目读取问题。

### 4. 快速扫描和深度扫描有什么区别？

**快速扫描**完全在本地运行，不调用 AI、不消耗模型 Token。它根据日期、时间线标记和相关文字特征筛选候选，并做基础日期整理；不会自动完成可靠的语义分组、边界推导或命名。候选默认未选中且为低置信度，不会直接纳管；请先勾选需要管理的条目，再逐项点击“检查”，确认标题、开始日期和边界并保存。

**深度扫描全部条目**会把当前绑定世界书的全部条目交给 AI 分析，负责候选识别、分组、排序、标题、日期和边界建议。条目较多时会分批处理，再汇总结构化结果。

两种扫描都会先生成草稿。草稿经过人工检查并点击“确认并应用”后，才会建立正式时间线配置。如果当前聊天已经保存了合法故事时间，应用后会立即校准自动组，可能马上改变受管条目的启用状态。

## AI 分析与输入边界

### 5. AI 分析失败了，应该怎么排查？

先确认使用的是深度扫描；快速扫描不需要 AI。然后按以下顺序检查：

1. 打开“设置 → AI 分析”，确认提供商、模型、URL 和凭据配置正确。
2. 点击“测试连接”，先确认接口能返回结果。
3. 查看“日志 → 系统日志”和分析页面的错误提示；如果页面提供“查看最后一次原始输出”，可在本地检查，但模型可能在异常结果中复述世界书输入，复制、保存或反馈前必须先检查并打码。
4. 先把破限提示词和固定提示词重置为默认值，再重新分析。
5. 仍失败时，降低 Temperature、适当增加最大输出 Token 或请求超时后重试。

快速扫描可以先用来确认世界书能够正常读取；它成功不代表 AI 接口已经配置成功。

### 6. 提示“没有可用模型”怎么办？

如果选择“跟随主 API”：

- 先确认 SillyTavern 主 API 已连接并有当前模型。
- “模型选择”留空时，插件会使用 SillyTavern 当前主 API 的模型。
- 可以点击“获取模型”；部分主 API 不提供模型列表，此时列表为空不一定表示主 API 不能生成，优先检查主 API 当前模型是否可用。

如果选择“副 API”：

- 填写 API URL 和 API Key，点击“保存 AI 设置”。
- 点击“获取模型”，从返回列表中选择模型，或在“模型名称”中填写接口要求的模型 ID。
- 再点击“测试连接”，成功后重新进行深度扫描。

### 7. AI 返回非法 JSON 怎么办？

插件会对结构化结果自动校验，最多共尝试 3 次，也就是首次请求失败后最多再进行 2 次修复请求。仍失败时，分析页面会显示错误；如果有原始输出，可以展开查看最后一次结果。

可执行的处理方式：

1. 重置破限提示词和固定提示词并保存。
2. 降低 Temperature，增加最大输出 Token。
3. 确认模型支持稳定输出 JSON，不要在自定义提示词中要求 Markdown 或额外解释。
4. 重新运行深度扫描。

非法 JSON 不会直接写入时间线配置，也不会修改世界书。

### 8. AI 请求超时怎么办？

打开“设置 → AI 分析”，增加“请求超时（秒）”，保存后重试。默认超时时间为 180 秒。

同时检查：

- SillyTavern 主 API 或副 API 是否仍在线；
- 副 API URL 是否正确；
- 模型名称是否真实存在；
- 网络或反向代理是否限制了请求时间。

如果只是想确认世界书读取正常，可以先使用不需要 AI 的快速扫描。

### 9. AI 会读取聊天正文吗？

不会。AI 分析请求不读取、不请求也不发送当前或历史聊天消息、角色卡正文、Persona、示例对话、`chatMetadata` 或 `<wlog>` 所在消息。

聊天正文只有两个用途：AI 回复完成后读取末条 AI 消息中的 `<wlog>`，以及用户点击“获取当前时间”时重新读取末条 AI 消息。读取结果只用于确定故事时间，不进入 AI 分析链路。

### 10. AI 实际读取哪些世界书字段？

深度扫描发送的世界书原始数据严格只有三个字段：

- `entryId`：条目 ID；
- `comment`：原条目名称/Comment；
- `content`：当前绑定世界书的条目正文。

这里的 `content` 指世界书条目正文，不是聊天消息正文。`enabled`、Key、Secondary Key、Order、Depth、Position、Probability、Strategy 和其他原生字段不会作为 AI 分析输入透传。

AI 还会收到插件默认或自定义的破限提示词、固定提示词以及当前分析指令；这些是分析指令，不是聊天上下文。

### 11. 世界书里的 SillyTavern 宏会不会被执行？

不会。世界书条目和自定义提示词中的 `{{...}}` 宏按普通文本处理，发送前会阻止宿主展开，不能通过 `{{lastMessage}}` 等宏把聊天正文带入分析。

因此不要依赖宏把当前日期传给 AI。运行阶段的日期来源仍然是合法的 `<wlog>`。

### 12. “确认并应用”按钮不可点怎么办？

通常是草稿仍有待处理项目，或者当前草稿已经不属于当前角色/世界书。重点检查：

- “待确认”数量是否大于 0；
- 中置信度条目是否仍未确认；
- 低置信度条目是否未人工检查；
- 是否还有警告、缺失开始日期或非法边界日期；
- 同一条目是否被多个分组重复纳管；
- 当前角色或绑定世界书是否在扫描期间发生变化。

点击对应条目的“检查/编辑”，补全标题、开始日期和边界，或取消选择不确定条目。完成后再次确认；不要通过刷新页面绕过校验。

### 13. 低置信度和“待确认”是什么意思？

低置信度表示插件或 AI 无法可靠判断该条目是否属于时间线、日期或边界。它默认不会被采用；如果你要将它纳入管理，必须先人工编辑并确认。中置信度或带警告且已选中的条目会计入“待确认”。

请打开条目的“检查/编辑”，核对正文、标题、开始日期和边界：确认无误后保存人工修改，或取消勾选该条目。只有所有纳管条目通过检查并点击“确认并应用”后，才会参与自动切换。

## 故事时间与 `<wlog>`

### 14. “获取当前时间”失败怎么办？

该按钮只检查当前聊天最近一条 AI 回复，不会跳过它去读取更早消息。失败时插件保持原有故事时间和世界书开关不变。

请让 AI 重新生成一条完整回复，确保末条 AI 消息包含合法的 `<wlog time="...">`，然后再次点击“获取当前时间”。如果仍失败，以页面显示的失败原因为准；单纯缺失或格式非法不一定会产生单独的系统日志。

### 15. 什么是合法的 `<wlog>` 格式？

推荐使用以下形式：

```xml
<wlog time="🕒时间:424年5月14日/17:15">
故事日志内容
</wlog>
```

年、月、日必须是有效日期；时和分可以省略，若提供则小时为 `0～23`、分钟为 `0～59`。标签必须完整闭合，`time` 属性必须使用引号。插件按年/月/日切换世界书，时/分只用于显示和记录。

### 16. 一条消息有多个 `<wlog>` 会用哪一个？

插件从消息末尾向前检查，使用最后一个**完整且合法**的 `<wlog time>`。如果最末标签损坏，但前一个标签合法，就使用前一个合法标签；如果全部无效，则沿用旧时间或保持未接管状态。

### 17. 我手工编辑了 AI 正文，为什么时间没有自动变化？

这是保护行为。打开或切换聊天时，插件优先使用已保存的 `chatMetadata`，不会因为手工修改末条消息就自动覆盖已保存时间。

手工编辑后请点击“获取当前时间”。该按钮会重新读取当前聊天末条 AI 消息；如果新日期早于旧日期，还会进入时间倒退确认。

## 匹配、开关与运行模式

### 18. 检测到时间倒退怎么办？

插件会显示“检测到时间倒退”，提供“保留原时间”和“应用新时间”两个选择。在你作出选择前：

- 当前故事时间不变；
- 自动时间线不切换；
- 新的时间不会覆盖待确认状态。

确认应用后，插件才会按新日期重新计算自动组；拒绝则继续使用原时间。

### 19. 出现无匹配、冲突或空档怎么办？

插件不会选择“最接近”的条目，也不会调用 AI 猜测。对应分组会被安全阻断，当前世界书开关保持不变。

- **无匹配**：当前故事日期或配置无法得到有效目标。
- **冲突**：多个条目起点相同或有效范围重叠。
- **空档**：相邻条目的有效日期范围之间存在缺口。

进入“分组”或重新分析草稿，修正排序、开始日期和边界，确认并应用后再同步。其他没有问题的分组仍可独立运行。

### 20. 自动模式和手动模式有什么区别？

每个分组单独保存模式：

- **自动**：插件按当前故事日期控制该组受管条目，同组最多启用一个正确条目；卡片开关只展示真实状态，不能直接点击。
- **手动**：插件停止自动干预该组，用户可以在组内任意组合开关。

自动切换到手动时保持当前开关原样。手动切换到自动时，如果当前聊天有合法保存时间，会立即按该时间校准；没有合法时间则不写入。

### 21. 世界书开关没有变化，是不是插件失效了？

先检查以下常见原因：

- 同一天或仍命中同一条，插件只更新时间，不重复写入；
- 当前分组处于手动模式；
- 没有合法故事时间；
- 分组存在无匹配、冲突、空档、重复纳管或 stale 阻断；
- 当前标签页没有世界书控制权；
- 写入或写入后校验失败。

查看当前故事时间、分组模式、页面提示和系统日志。如果异常条目界面提供“重新同步”，可在修正配置和时间后使用；其他页面不一定显示该入口。异常未解决前插件会保持原开关，避免误切换。

### 22. 我在 SillyTavern 原生界面手动改了开关，会怎样？

自动模式不会即时拦截外部修改。下一次正常自动同步时，插件会重新计算目标并恢复自动组应有的状态，同时记录日志。

如果希望长期保留手工状态，请先把该组切换为手动模式。手动模式下插件不会替你改回去。

### 23. 多个标签页同时打开时谁能控制世界书？

同一本世界书同一时刻只允许一个标签页持有自动控制权。没有控制权的标签页会显示“其他标签页正在控制当前世界书”，并阻止自动写入。

排查步骤：

1. 关闭其他正在使用同一本世界书的 SillyTavern 标签页。
2. 在当前页面点击“接管控制权”。
3. 看到当前标签页重新取得控制权后，再执行同步。

### 24. 世界书条目正文变了，或条目 ID 变了，怎么办？

正文变化会被内容 Hash 标记为 `stale`，旧配置先保留，插件会提示重新分析；不要把旧配置当成新正文已经验证。

条目被删除或 ID 改变时，插件不会只凭名称自动接管新条目。重新分析时可能出现按正文 Hash/相似度匹配的继承建议；只有用户确认后才继承旧配置的人工字段。无法可靠匹配的旧映射会保留为待处理状态并停止相关自动切换。

建议流程：

1. 保留当前配置，不要直接删除旧分组。
2. 对当前绑定世界书重新分析。
3. 核对差异和疑似重匹配提示。
4. 确认可靠的匹配，修正其余条目后再“确认并应用”。

### 25. 删除分组后为什么没有“未分组”兜底？

这是设计如此。删除分组只删除该分组及插件中的映射，不会修改世界书正文或其他原生字段，也不会自动创建“未分组”。

需要继续管理被删除分组中的条目时，请重新分析并确认配置，或使用“新建分组”后人工移动条目。未重新纳管的条目不会被自动切换。

## 副 API、提示词与主题

### 26. 副 API 怎么保存？

进入“设置 → AI 分析”，选择“副 API”，然后：

1. 选择已有共享连接，或点击“新建副 API”。
2. 填写连接名称、API URL、API Key 和模型名称。
3. 点击“保存 AI 设置”。
4. 点击“测试连接”确认可用。

留空已保存的 API Key 表示不修改原 Key。API Key 不保存到理脉普通设置中，而是交给 SillyTavern Secrets 管理。

### 27. “获取模型”失败怎么办？

副 API 先确认 URL、API Key 和接口支持模型列表查询，再点击“获取模型”。列表为空或 HTTP 错误时，检查接口地址、凭据和反向代理权限；也可以直接在“模型名称”中填写该接口要求的模型 ID。

主 API 的模型列表由 SillyTavern 当前主 API 提供。部分主 API 不支持列表接口，此时可以让模型选择留空，使用 SillyTavern 当前模型，并用“测试连接”验证。

### 28. YaKit-理脉和 YaKit-纪实如何共享副 API？

两者使用 YaKit 家族共享的副 API 连接。任一插件保存连接后，另一插件重新打开设置或刷新连接列表，就可以按连接名称选择并使用同一 URL、模型和 Secrets 引用。

如果另一端看不到新连接：

1. 确认保存操作显示成功。
2. 关闭并重新打开设置页或刷新 SillyTavern。
3. 在副 API下拉列表中重新选择连接。

共享的是连接配置和 Secrets 引用，不会把 API Key 明文复制到普通插件设置中。

### 29. 破限提示词、固定提示词如何重置？“已修改”是什么意思？

进入“设置 → 自定义提示词”，点击对应的二级容器：

- **破限提示词**位于发送顺序第一位；
- **固定提示词**位于破限提示词之后；
- 当前分析请求最后发送。

卡片和编辑弹窗会显示“默认”或“已修改”。内容与内置预设不同就是“已修改”。点击弹窗左下角“重置”只会把当前弹窗内容填回预设，随后必须点击“保存提示词”才会持久化；点击取消不会保存重置结果。

### 30. 主题跟随是跟随什么？

“设置 → 常规 → 主题”中的“跟随 SillyTavern”会直接继承 SillyTavern 的主题变量，不会自行判断系统亮暗色，也不会修改 SillyTavern 全局主题。

选择“跟随”“浅色”或“深色”后会立即应用并保存。若颜色异常，先切换到浅色或深色确认插件本身正常，再检查 SillyTavern 当前主题。

## 更新、数据与隐私

### 31. 更新按钮怎么用？会不会直接更新？

进入设置时插件会自动检查更新；设置右侧也会显示当前版本按钮，可手动重新检查。

发现更新后会出现“更新”按钮。只有用户点击该按钮才会执行更新，不需要二次确认；更新成功后会自动刷新 SillyTavern 页面。检查到“当前已是最新版本”时不会执行更新。

### 32. 更新失败怎么办？

更新由 SillyTavern 宿主的 Git 更新能力完成。失败时，插件不会显示成功或自动刷新，但是否完整保留旧文件取决于宿主的实际更新结果，YaKit-理脉不能承诺原子回滚。

先记录设置页当前版本区域显示的错误，再检查 SillyTavern 是否能访问 GitHub、扩展是否由 Git 安装，以及当前账号/网络是否允许拉取仓库。确认仓库地址为 <https://github.com/git-yafaya/ST-YaKit-Timeline.git> 后，可使用 SillyTavern 扩展管理器的常规更新能力重试；不要删除聊天数据或世界书来排查更新问题。

### 33. 如何导出或导入时间线配置？

进入“设置 → 数据管理”：

- “导出 JSON”只导出当前绑定世界书已经建立的时间线配置；不包含 AI 设置、API Key、当前聊天时间、各聊天的运行模式或日志。
- 导入前必须先选择角色并绑定配置所属的世界书。插件会校验世界书、条目 ID、正文 Hash、缺失项和可能的条目重匹配，不会直接把未校验文件写入。
- 阅读差异与确认提示后，明确确认才会保存导入配置。若当前聊天已有合法故事时间，导入完成后会立即重新校准自动组，可能改变受管条目的启用状态。

### 34. 配置、聊天状态和 API Key 保存在哪里？

插件不使用 `localStorage` 保存正式数据：

- 全局设置和世界书时间线配置保存于 SillyTavern 的 `extension_settings.yakit_timeline`；
- 当前聊天的故事时间、分组模式、激活项和运行日志保存于 `chatMetadata.yakit_timeline`；
- 副 API Key 保存于 SillyTavern Secrets，插件普通设置只保存 Secrets 引用。

导出配置不会包含 API Key。不要把 API Key 写进提示词、日志、截图或反馈内容。

### 35. 日志会保存聊天正文或世界书正文吗？

不会。运行日志与系统日志分开显示和记录，且都不记录 API Key、完整聊天正文或完整世界书正文。

- 当前聊天的运行日志保存在该聊天的 `chatMetadata` 中，最多保留最近 100 条。
- 系统日志只存在于当前前端运行会话，刷新页面后不会恢复。

反馈问题时仍请自行检查复制内容，删除角色名、聊天原文、世界书正文、URL 中的密钥和任何 Secrets 信息。

## 移动端与反馈

### 36. 移动端点击魔法棒或 YaKit-理脉没有反应怎么办？

`0.2.3` 当前以 PC 端流程作为主要验收环境，移动端最终触控适配仍可能存在入口点击或布局不稳定。遇到点击无响应时，请先在 PC 浏览器完成配置、扫描和运行验收，不要反复修改世界书或 API 设置。

如果要记录移动端问题，保留设备/浏览器、SillyTavern 版本、点击位置和是否能打开魔法棒菜单等信息；不要上传聊天正文或 API Key。

### 37. 如何反馈问题？

请先阅读 [readme.md](readme.md)，再到 GitHub Issues 反馈：<https://github.com/git-yafaya/ST-YaKit-Timeline/issues>。

反馈内容建议包括：

- YaKit-理脉版本（当前为 `0.2.3`）；
- SillyTavern 版本和浏览器；
- PC 或移动端、主 API 或副 API、快速或深度扫描；
- 可复现的操作步骤；
- 页面提示、系统日志错误和是否能用“测试连接”成功。

请先打码或删除 API Key、Secrets、完整聊天正文、角色卡正文和世界书正文；只提供足以定位问题的最小片段。
