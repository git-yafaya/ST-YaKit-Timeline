# 工作日志

## 2026-08-18

- [x] SillyTavern 扩展部署结果：已验证。
- [x] 完成故事日期、`<wlog>` 解析和确定性时间线匹配纯逻辑；核心边界测试通过。
- [x] 按最新 HTML 视觉基线完成公共 Shell 组件化：拆分顶部导航、上下文状态栏和内容插槽，并同步部署。
- [x] 将 `overview-review.html` 正文转换为类型化 `OverviewPage.vue`，支持配置卡片、异常检查和未配置扫描引导。
- [x] 将 `时间线.html` 转换为类型化 `TimelinePage.vue`，支持搜索筛选、组模式事件、条目开关事件、冲突操作与正文只读预览。
- [x] 将 `分组.html` 转换为类型化 `GroupPage.vue`，支持新建、重命名、合并、拆分、删除与排序事件，并提供无配置引导状态。
- [x] 将 `AI分析.html` 转换为类型化 `AnalysisPage.vue`，覆盖未扫描、扫描进度与草稿确认状态，所有正式写入均保留为外层事件。
- [x] 将 `日志.html` 转换为类型化 `LogsPage.vue`，隔离当前聊天运行日志与插件系统日志，并限制运行日志视图为最近 100 条。
- [x] 将 `设置s.html` 转换为类型化 `SettingsPage.vue`，覆盖常规、AI、自动切换和数据管理，并保持 API Key 与导入导出的安全边界。
- [x] 取消 SillyTavern 最低版本限制；扩展清单不再声明 `minimum_client_version`，具体兼容问题出现后再修复。
- [x] 固定时间线主窗口尺寸；Tab 切换只更新并滚动中间内容区，顶部导航与上下文栏保持不动。
- [x] 冻结顶部 Tab 的交互几何；Normal、Hover、Active、Focus 仅通过颜色、边框、阴影和伪元素反馈状态。
- [x] 为 Tab 状态和内容切换加入 180ms `ease-out` 轻量过渡，动画仅涉及颜色、底部指示条、透明度与 4px 位移。
- [x] 将时间线页与设置页原生选择器替换为深色自定义 Listbox，并统一主内容区与下拉层的细窄深色滚动条。
- [x] 完成 Light/Dark 语义 Token 主题系统；`follow` 按 SillyTavern 实际主题亮度实时映射，手动主题写入扩展全局设置，组件颜色全部改用 CSS Variables。
- [x] 主题 Listbox 改为选择后实时应用并持久化，不再依赖常规设置保存按钮。
- [x] 仅将设置 Main Content 重构为居中单列 Accordion，保留原固定 Shell、顶部导航、状态栏、窗口尺寸、内容滚动边界及全部设置字段与事件。

## 2026-08-19

- [x] 将桌面设置 Accordion 收紧为 960px 居中高密度列表，压缩卡片、间距、展开内容和页面上下留白，不改固定 Shell 与业务逻辑。
- [x] 将 AI 接口提供商文案调整为“跟随主 API / 副 API”，补充接口用途副标题并明确模型留空时跟随主 API 当前模型。
- [x] 将大跨度时间跳跃提醒阈值默认值改为 5 天，并限定为 5～30 天、步长 5 天的自定义下拉选项。
- [x] 接通常规、AI 与自动切换设置的完整 SillyTavern 持久化；启动时统一恢复并校验配置，分区保存不覆盖世界书及其他插件字段。
- [x] 为主 API 与副 API 增加模型获取和自定义 Listbox，副 API 选中模型后同步模型名称；新配置默认参数调整为 Temperature 0.9、最大输出 23333 Token、超时 180 秒。
- [x] 修复“测试连接”空事件并复用模型状态接口执行真实检查；AI 设置保存改为返回结果并显示成功或失败反馈，不再静默失败。
- [x] 实现只读 SillyTavernAdapter，仅按当前角色卡主绑定读取世界书和条目，并将当前角色/世界书状态接入顶部状态栏；角色、聊天及世界书变化时安全刷新。
- [x] 将总览空状态接入真实角色世界书读取结果，区分无角色、未绑定、不可读、空世界书和未配置；仅在当前绑定世界书可读且含条目时开放扫描入口。
- [x] 接通 AI 扫描草稿链路：支持快速候选筛选、深度扫描、25 条分批汇总、严格 JSON / 日期 / entry ID 校验、三次修复重试、进度、取消及角色作用域过期保护；主 API 与副 API 均通过 SillyTavern 能力发送，确认前不写世界书。
- [x] 接通首次“确认并应用”：将合格草稿转换为带 SHA-256、人工锁定和确定性有效范围的世界书公共配置，按 worldbookKey 分区安全保存；总览与时间线读取正式配置和真实开关，应用过程不修改任何世界书原生字段，已有配置禁止直接覆盖。

## 2026-08-20（聊天状态与时间恢复阶段）

- [x] 新增 `chatMetadata.st_yafaya_timeline` 聊天状态适配器，严格校验故事时间、组状态、倒退待确认状态和日志结构；保存只通过 SillyTavern 元数据接口，不使用 `localStorage`。
- [x] 新增末条 AI 消息恢复逻辑：只读取当前聊天最后一条 AI 消息，metadata 有效时间优先，缺失/未初始化时才从最后一条合法 `<wlog>` 恢复。
- [x] 接入 SillyTavern `MESSAGE_RECEIVED` 完成事件；流式生成期间不解析，生成中止或末条 `<wlog>` 无效时保持原时间和状态。
- [x] 接入“获取当前时间”按钮、同日更新时间、大跨度提醒和时间倒退待确认/确认/拒绝交互；本阶段仍不写入世界书原生 `enabled`。
- [x] 增加聊天状态、末条消息、倒退和恢复优先级测试；验证 83 项测试、类型检查、Vite 构建和 `git diff --check`。
- [x] 接入确定性自动引擎与安全世界书写入适配器：仅修改当前角色实际绑定世界书中受管条目的 `disable`，保存前复核绑定，保存后复读校验并最多重试一次；组级异常互不影响。
- [x] 接入聊天级自动 / 手动模式、激活条目、运行日志、系统日志、多标签 BroadcastChannel 控制权与明确接管；同日不写世界书，倒退需确认，缺失时间不接管。
- [x] 接入正文 Hash 变化检测、旧配置安全保留、重新分析差异视图与人工字段保护；补齐分组新建、重命名、合并、拆分、删除转未分组、排序、跨组拖拽和安全配置导入导出。
- [x] 完成 V1 验收前静态检查：类型检查、92 项测试、生产构建、diff 检查、Docker 挂载与构建文件 SHA-256 校验；实际 HTTP 页面因本机 SillyTavern Basic Auth 未提供会话而无法无头打开。
